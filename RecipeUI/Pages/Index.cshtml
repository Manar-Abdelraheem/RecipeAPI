@page
@model IndexModel
@{
    ViewData["Title"] = "Home page";
}

<div class="text-center">
    <h1 class="display-4">Recipes Application</h1>
</div>
<div class="col-container">
            <h2 class="col">
                @foreach(var recipe in Model.Recipes)
                {
               <button class="button">@recipe.Title</button>
                   
                }
            </h2>
</div>